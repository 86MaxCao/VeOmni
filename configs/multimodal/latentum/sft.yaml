model:
  model_path: /mnt/nas-tbt/tbt/checkpoint/hf_cache/LatentUM-Base
  ops_implementation:
    attn_implementation: flash_attention_2

data:
  train_path: configs/multimodal/data/tulu_sharegpt4v.yaml
  data_type: conversation
  chat_template: qwen3vl
  max_seq_len: 4096
  train_size: 80000000
  mm_configs:
    image_max_pixels: 602112

train:
  accelerator:
    ulysses_size: 1
    fsdp_config:
      fsdp_mode: fsdp2
  gradient_checkpointing:
    enable_reentrant: false
  wandb:
    enable: false
    project: latentum_sft
    name: latentum_sft
  freeze_vit: true
  freeze_vae: true
  freeze_gen_modules: true
  optimizer:
    lr: 1.0e-5
    lr_decay_style: cosine
  num_train_epochs: 1
  micro_batch_size: 1
  global_batch_size: 8
  max_steps: 50
  init_device: meta
  checkpoint:
    output_dir: latentum_sft_output
    manager: dcp
    save_hf_weights: false
